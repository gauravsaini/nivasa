# CRUD REST Example

Minimal Nivasa CRUD example with `GET`, `POST`, `PUT`, and `DELETE` routes.

## Run

```bash
cargo check --manifest-path examples/crud-rest-api/Cargo.toml
```

This example builds the app shell and route metadata. It stays intentionally
small so the CRUD shape is visible without adding runtime storage yet.
