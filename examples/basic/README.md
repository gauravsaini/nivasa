# Basic Example

This placeholder now points to [`examples/hello-world/`](../hello-world), which
shows the current minimal starter flow with a real app shell and one route.

## Run

```bash
cargo run --manifest-path examples/hello-world/Cargo.toml
```
