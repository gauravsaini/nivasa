# WebSocket Chat Example

Minimal Nivasa chat-style WebSocket example.

## Run

```bash
cargo run --manifest-path examples/websocket-chat/Cargo.toml
```

This example keeps the transport side minimal and demonstrates the gateway
metadata + room registry surface that the framework exposes today.
