[package]
name = "nivasa-example-testing"
version = "0.1.0"
edition = "2024"

[dependencies]
nivasa-core = { path = "../../nivasa-core" }
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }

[workspace]
